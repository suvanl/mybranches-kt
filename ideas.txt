future ideas/enhancements:

- search (fuzzy) for branch in list

- the user can specify a flag which will first delete any local branches gone from remote before listing the
    remaining local branches

- allow the user to provide a custom username and/or branch name pattern

- option to include main/master branch in the list

- custom branch list sort order (e.g. alphabetical instead of by committer date)

- extract a default reusable CommandRunner impl (e.g. DefaultCommandRunner) for non-test usage
    - if we ever need it anywhere outside of GitClient

- improve compilation time:
    - see https://kotlinlang.org/docs/native-improving-compilation-time.html
    - if all else fails... try customising LLVM: https://kotlinlang.org/docs/native-llvm-passes.html

- colours/theme: support both light and dark theme terminals
    -> use `LocalTerminalState.current.theme` to get current theme
